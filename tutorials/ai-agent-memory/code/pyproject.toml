[project]
name = "ai-agent-memory-demo"
version = "0.1.0"
description = "Small teaching demos for AI agent memory."
requires-python = ">=3.11"
dependencies = [
  "openai>=1.93.0",
]

[project.optional-dependencies]
mem0 = [
  "mem0ai>=0.1.0",
]
