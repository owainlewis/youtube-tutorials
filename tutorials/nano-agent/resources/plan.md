So we're building a terminal-based minimal coding agent. It runs in the terminal, we're going to build this using Python and the Anthropic API. This is going to be a mini version of a tool like ClaudeCode. A user will be able to start it in the terminal, and the agent will be able to perform actions such as reading and writing files and editing files, and it will also have the ability to execute bash commands. The primary user of this is a developer who is learning how to build coding agents from scratch. So we want to keep the initial build relatively simple. We also want a basic UI, so we want a nice-looking text UI, nice-looking terminal UI maybe with the rich library from Python. The goal of this agent is to be teaching people how to build their agents and so we want to keep things relatively simple in terms of the implementation. We won't do any streaming but we do want the ability to see the agent's thinking trace, so when the agent is thinking we want to be able to see that in the terminal and we also want to be able to see the tool calls that are being made. This is really important because we need to make sure that we can see which tools are being called.